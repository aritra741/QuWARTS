-- Query 1: select (player)
SELECT draft_pick FROM player;

-- Query 2: select (player)
SELECT team, name, mvp_awards, birth_date FROM player;

-- Query 3: select (team)
SELECT ownership, team_name, championship, founded_year FROM team;

-- Query 4: select (city)
SELECT area, city_name, gdp FROM city;

-- Query 5: select (owner)
SELECT nba_team, name, own_year FROM owner;
